# Copy the right template for each environment:
#   Local:       .env.example → .env.local
#   Vercel Prod: .env.production.example  → Vercel (Production checkbox)
#   Vercel Prev: .env.preview.example     → Vercel (Preview checkbox)
#
# Full guide: docs/ENV.md

# --- Coming soon launch mode ---
# Set true to keep the public site visible while replacing order CTAs with reminders
# and blocking checkout server-side.
# COMING_SOON_MODE=true
